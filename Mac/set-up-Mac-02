* login as user/Administrator
* set-up password
* Enter wireless password
* do App Store update if needed
* download Google Chrome (from Safari)
* while waitng, make Dock auto disappear
  (settings, Dock, auto)
* short-cut for system config is Option-F-key
* double click googlechrome.dmg and move the app to App folder
* install brew (google "install homebrew Mac")
  ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
* brew install git
* download and install Firefox (56.0.2. dmg open install)
... see set-up version 01
